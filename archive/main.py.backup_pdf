import tkinter as tk

root = tk.Tk()

root.title("BookBuilder")

root.geometry("700x500")

label = tk.Label(
    root,
    text="BOOKBUILDER",
    font=("Arial",28,"bold")
)

label.pack(pady=40)

root.mainloop()
